import { handleStoreDealsCron } from '../../server/storeDealsCronHandler.js';

/** Per-store deal refresh when a retailer window opens (e.g. ALDI Sat Special Buys). */
export default handleStoreDealsCron;
